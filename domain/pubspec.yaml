name: domain
description: Flutter domain module.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  injectable: ^1.5.4
  get_it: ^7.2.0
  messagepack: 0.2.1
  cryptography: ^2.0.5
  uuid: ^3.0.7
  path_provider: ^2.0.11
  http: ^0.13.5

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  injectable_generator:
  build_runner:

flutter: